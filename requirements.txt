# Interface graphique
customtkinter>=5.2

# API Clash of Clans & données
requests>=2.28
pandas>=1.5
pyarrow>=10.0
# NumPy 1.x n'a pas de wheel précompilé pour les versions récentes de Python
# (ex. 3.14) : pip retombe sur une compilation source, qui échoue sans
# compilateur C installé. pyarrow (non plafonné ci-dessus) gère NumPy 2 depuis
# la 16.x, donc plus besoin de brider NumPy à la branche 1.x.
numpy>=1.23
# Moteur Excel de pandas : classeurs de surveillance (Surveillance/<TAG>.xlsx),
# feuilles _meta des scans et exports .xlsx. pandas ne l'installe pas seul.
openpyxl>=3.1
python-dotenv>=1.0
tqdm>=4.64
matplotlib>=3.6

# Automatisation souris/clavier & capture d'écran
pynput>=1.7
pyautogui>=0.9
pyperclip>=1.8
dxcam>=0.0.5
pillow>=9.0

# OCR
pytesseract>=0.3
opencv-python>=4.6
easyocr>=1.6
